<%= form_for(@tipo_egreso) do |f| %>
  <% if @tipo_egreso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_egreso.errors.count, "error") %> prohibited this tipo_egreso from being saved:</h2>

      <ul>
      <% @tipo_egreso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
