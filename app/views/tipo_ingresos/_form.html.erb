<%= form_for(@tipo_ingreso) do |f| %>
  <% if @tipo_ingreso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_ingreso.errors.count, "error") %> prohibited this tipo_ingreso from being saved:</h2>

      <ul>
      <% @tipo_ingreso.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
